## Summary

1.2 machines match the id/sale/save contracts: no whisky, no `sugar.count`, cane harvests as fruit, mill/jam/still/barrel/freezer SKUs gate on existing research, `SAVE_VERSION`/`PROTOCOL` 1.2 with no migrate. Hopper dump is compost-style, ticks are cell-owned, freezer skips rot. Look/prompt and shop still miss the machines tables: still/barrel status is hidden by hand, jam tomato lock uses the crop id, inspect doubles the look line, `buy-sugar` skips the inventory-full gate, `itemInner` falls back to sugar.

## Issues

### Issue 1 — Severity: bug
- File: src/game/sim/prompt.ts:438
- Description: `stillLook` checks overflow and wrong-crop refuse before `progress > 0`. A working still (`n === STILL_CAP`) with potatoes in hand shows **Pot still - full**; with any other fruit, **Pot still - potatoes, wheat or apricot**. `barrelLook` does the same: maturing/aging with grapes shows **Wine barrel - full**; with other fruit, **Wine barrel - grapes**. ui/machines working/aging rows never appear while the gardener is holding dump cargo.
- Suggestion: After refuse-wrong, if the machine is working (still `progress > 0`) or barrel is at cap, emit working/maturing/aging. Keep **full** / refuse for dump-illegal idle states only.
- Status: fixed

### Issue 2 — Severity: bug
- File: src/game/sim/prompt.ts:479
- Description: Locked jam look is ``Jam machine - ${jam.crop} only``. Tomato is `tomato`, so ketchup lock is **Jam machine - tomato only**. ui/machines `{crop}` / `{name}` is ketchup for tomato (`jamCropName`). Filling already uses `jamCropName`; lock does not.
- Suggestion: Use `jamCropName(jam.crop)` on the lock line.
- Status: fixed

### Issue 3 — Severity: bug
- File: src/game/sim/look.ts:73
- Description: Cell title is `millLook` / `stillLook` / `barrelLook` / `jamLook`, then `lookText` always appends `prompt().text`. Dump illegal → prompt is that same look line. Inspect is **Mill - full** twice (empty-hand full mill), **Pot still - 3/10** twice, etc. Compost keeps a short cell name and puts status only on the prompt.
- Suggestion: Cell title **Mill** / **Pot still** / **Wine barrel** / **Jam machine** (freezer already **Freezer**). Leave millLook et al. on the prompt path only.
- Status: fixed

### Issue 4 — Severity: bug
- File: src/game/ui/shop.tsx:210
- Description: `buy-sugar` does not arm; it goes to inventory like grass-seeds. `rowState` checks grass-seeds and packs for a free/merge slot, then returns `ok`. Full house: sugar row stays clickable, `buy()` returns `Inventory full`, UI is silent. No **No room in the inventory**.
- Suggestion: Same empty/merge test as grass-seeds for `made.kind === 'sugar'` (`canFitSugar`).
- Status: fixed

### Issue 5 — Severity: bug
- File: src/game/view/svgs.ts:318
- Description: `itemInner` ends `return inner(itemSugar)` after every `Face` arm. Silent sugar fallback for a missed kind. Canon: no recovering default.
- Suggestion: Exhaust `Face`; `const _x: never = item` (or equivalent) and drop the sugar return.
- Status: fixed
