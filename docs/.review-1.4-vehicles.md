## Summary

Vehicles I matches the id/cmd/save surface: `SAVE_VERSION`/`PROTOCOL` 1.4, Quad hangar-buy at `QUAD_PRICE`, guest parity, no tractor/trailer/attachments, `HudTarget` still sprinkler-only. Tests passing does not cover the holes. Store-on-pad never snaps the gardener; `dest` recovers missing/stored vehicles; the menu wordmark is still 1.3.0; `intents.ts`/`field.ts` (and the other untracked world extracts) are dead copies while `World` grew the live slice.

## Issues

### Issue 1 — Severity: bug
- File: src/game/sim/intents.ts:218
- Description: Architect table gives `intents.ts` hangar/vehicle/embark and `field.ts` vehicle `tickFreshness`. Nothing imports those modules. `World.begin` / `World.tickFreshness` / `World.tickQueue` still own the live paths (`world.ts:2944`, `world.ts:3229`, `world.ts:2775`). Same unfinished split: `build.ts`, `util.ts`, `watergrid.ts`. The copies already drifted — `intents` embark does not refuse an already-driving seat and does not `board()` (no `workLeft`/`filling` clear); `world.ts:2963` does. Dead layer plus a 400-line dump into an already-huge `World`.
- Suggestion: One owner. Wire `World` to the extracts and delete the class copies, or delete the extracts. `tickVehicles` belongs next to `integrateVehicle`, not inlined. Do not keep both.
- Status: open — out of slice. Untracked extracts predate Vehicles I. Not wired.

### Issue 2 — Severity: bug
- File: src/game/sim/world.ts:2324
- Description: Store-on-pad sets `driver 'none'`, pose stored, clears drive/queue, `return`s. It does not write `driver.actor` to the vehicle’s last `x,y`. Coast-complete (`world.ts:2340`) and `board`/`deploy` do. Invariant 80: actor stays at vehicle `x,y` (standing on the pad). Crossing onto the pad this tick leaves the gardener at the pre-integrate cell. Hide-gardener then pops off-pad. Named hangar test never reads actor after store.
- Suggestion: Before the store `return`, `driver.actor.x/y = pose.x/y` (same as coast-complete). Assert it in the invariant 80 test.
- Status: stale — auto-store removed. `dockBody` writes actor to last x,y.

### Issue 3 — Severity: bug
- File: src/game/sim/world.ts:514
- Description: `dest` for `vehicle`/`embark`: field floor, else stored hangar origin, else the acting seat’s current cell. Canon: no recovering default. Missing id / already-stored should not invent a walk target so `inside(at)` succeeds and `begin` no-ops. Spec dest is the field floor at enqueue/apply; arrival then `shiftHead` if not parked.
- Suggestion: Field pose only. No hangar arm, no actor-cell fallback. Missing or stored → keep last dest or fail closed; `begin` already `shiftHead`s.
- Status: fixed — field floor only; else `{ Infinity, Infinity }` so `inside` fails and `begin` shiftHeads.

### Issue 4 — Severity: bug
- File: src/game/ui/menu.tsx:110
- Description: Wordmark is still **1.3.0**. `SAVE_VERSION` is 1.4, `PROTOCOL` is 1.4, `docs/ui/menu.md` locks **1.4.0**. The changelog toggle was rebuilt around that string and the number was not moved.
- Suggestion: **1.4.0**.
- Status: fixed — wordmark **1.4.0**.

### Issue 5 — Severity: bug
- File: src/game/sim/world.ts:2318
- Description: `surface = this.inWorld(at) ? surfaceMul(this.cell(at)) : SURFACE_NORMAL`. Out-of-world floor is an illegal pose; this papers it with identity mul. Same tick already rejects the step when `floor(nx,ny)` is unowned. `motion.ts:150` `vMax === 0 ? 0` is the same class (machinery mul is never 0).
- Suggestion: Current cell is owned. Call `surfaceMul(this.cell(at))`. Drop the `vMax === 0` branch.
- Status: fixed — `surfaceMul(this.cell(at))` only.

### Issue 6 — Severity: suggestion
- File: src/App.tsx:267
- Description: WASD sends only on keydown/keyup/blur. `send()` no-ops while not a driver, but `held` still updates. Deploy/embark while W is down never emits `Act.drive` until another key event. Spec is send-on-change, not per rAF — becoming driver is a change.
- Suggestion: After a successful local board/deploy (or when `driverVehicle` flips onto this seat), flush current `held` once.
- Status: fixed — rising-edge `wasDriver` flush.

### Issue 7 — Severity: suggestion
- File: src/game/ui/vehicle.tsx:40
- Description: `i as 0 | 1 | 2 | 3 | 4 | 5`. `VehicleSlot` already exists. Map index is `number` because `slots` is `Slot[]`, not a 6-tuple.
- Suggestion: Type `Vehicle.slots` as a 6-length slot array (or zip `VehicleSlot`). Pass `i` without a cast.
- Status: open
