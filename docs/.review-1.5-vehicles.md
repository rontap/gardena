## Summary

Vehicles II matches the id/cmd/save surface: `SAVE_VERSION`/`PROTOCOL` 1.5, wordmark **1.5.0**, `Vehicle`/`Trailer` unions, hangar-buys, stored tractor `hitch: 'none'`, parked-only `swapTrailer`, dash `{used}/100`, changelog 1.5. Discriminated unions are the right shape. Tests 160/160 do not cover the holes: field silos still grow a walk prompt, harvest boom is not actually asserted, and `??` / `find` recoveries paper over missing hitch/pose data.

## Issues

### Issue 1 — Severity: bug
- File: src/game/sim/prompt.ts:271
- Description: Invariant 84 / [[ui/vehicles]] look-prompt table: silo is look name only, no prompt act, no cue, no dialog. `readPrompt` handles parked + hangar then falls through. Empty hand hits `intent('Move here', { act: 'walk' })` at `:396`. Held junk hits `needSeeds`. `clickBody` therefore enqueues walk onto the solid footprint. `look.ts:122` always appends `world.prompt(at).text`, so inspect is `Seeding silo\nMove here` (or `Need seeds`). Cue-none is tested; prompt-none is not.
- Suggestion: After hangar, three silo arms `{ kind: 'blocked', text: 'Seeding silo' }` / spraying / produce. Do not walk. `lookText` should not append a second line for those cells.
- Status: open

### Issue 2 — Severity: bug
- File: src/game/sim/world.ts:4519
- Description: `harvestInsert` ends `slots[i] = kept[i] ?? { kind: 'empty' }`. Canon: `??` recovering missing data. The 8-slot array is a real length; empty tail is not an optional field. Same helper is the only boom harvest write path.
- Suggestion: Write `i < kept.length ? kept[i] : { kind: 'empty' }`, or compact into a fixed `HARVEST_SLOTS` tuple and assign. Prefer this next to `trailerUsed` in `vehicle.ts`.
- Status: open

### Issue 3 — Severity: bug
- File: src/game/sim/world.ts:2477
- Description: `hangarStores` types pose as `{ kind: string; hangar?: Coord }` and probes `hangar !== undefined`. Stored arms already require `hangar`. Optional soup to walk a union the type already distinguishes.
- Suggestion: `storedHere(p: VehiclePose | TrailerPose)` → `p.kind === 'stored' && p.hangar.col === origin.col && p.hangar.row === origin.row`.
- Status: open

### Issue 4 — Severity: bug
- File: src/game/sim/world.ts:1904
- Description: `taskName` vehicle arm: missing id or non-tractor → **Quad**. Dest for a missing/stored id is `{ Infinity, Infinity }`; the queue label still invents Quad. Same class as the boom heading fallback (`world.ts:2552`: `t.pose.kind === 'attached' ? t.pose.heading : v.pose.heading`) — caller already narrowed attached.
- Suggestion: Narrow `find` to the parked/driven tractor or quad; no default label. Boom: `t.pose.heading` only.
- Status: open

### Issue 5 — Severity: bug
- File: src/game/ui/vehicle.tsx:83
- Description: `i as 0 | 1 | 2 | 3 | 4 | 5` and `:105` `i as HarvestSlot`. `VehicleSlot` / `HarvestSlot` exist. Map index is `number` because `slots` is `Slot[]`, not an 8-tuple. `if (v === undefined) return null` (`:50`) is a defensive exit on a cue that already names the id.
- Suggestion: Tuple the slot arrays (or zip the slot-id unions). Drop the blank return; the cue is the vehicle.
- Status: open

### Issue 6 — Severity: bug
- File: src/game/sim/vehicle.test.ts:548
- Description: Boom harvest named test: `expect(w.cell(ripe).kind === 'empty' || w.trailers[0].kind === 'harvest').toBe(true)`. The trailer is always harvest. Plant may stay. Seed boom ORs four cells. Spray early-returns on `isSolid`. Hitch tests `if (kind !== 'tractor') return` and `pose.kind === 'field' ? pose.x : 0`. Silent pass / recovering 0. Invariant 84 boom bands are not pinned.
- Suggestion: After the tick, `expect(cell.kind).toBe('empty')` and assert the inserted fruit/seed/dead. Throw if the tractor is not a field tractor. No `||` success, no `return` skip.
- Status: open

### Issue 7 — Severity: suggestion
- File: src/game/sim/item.ts:545
- Description: Art locks hangar as industrial shed, not barn. `skuDesc('buy-hangar')` and almanac `catalog.ts:417` still say **3×2 barn**. Shop/almanac copy is house-language the art doc forbids. Tractor/trailers are only in the catalog blurb, not `skuDesc`.
- Suggestion: Shed, not barn. Quad + tractor (and trailers) in both strings, or drop the buy-list and keep footprint + hangar-buy.
- Status: open

### Issue 8 — Severity: suggestion
- File: src/game/sim/world.ts:2415
- Description: Spray swap no-ops `liters > TRAILER_CAP`. Spec is `floor(liters) > 100`. A bag at 100.4 is legal used (`floor` → 100) and rejected here. Seed path correctly uses `count`. Test only sends `101`.
- Suggestion: `Math.floor(hand.item.liters) > TRAILER_CAP`. Same predicate as `trailerUsed`.
- Status: open
