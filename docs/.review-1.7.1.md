## Summary

1.7.1 lands the named bricks: pulser / counter / day, lever `in` + rising-edge Flip, AND/OR/NOT dual-lock on `unlock-advanced-sensors`, `Act.stride`, dash occupied Faces, `QUAD_VMAX` 8, `SAVE_VERSION`/`PROTOCOL` 1.71. Discriminated cells and cmd letters match the contracts. Two canon holes: hydrate accepts illegal counter `n`, and dash cargo `find`s a hitch then paints none. Named invariant 117 has no test; 116 does not pin driver / recap.

## Issues

### Issue 1 — Severity: bug
- File: src/game/sim/save.ts:1588
- Description: Illegal: counter `n` not an integer in `1..COUNTER_MAX` as stored. `readSaveCell` takes any finite `num(o.n)` / `num(o.count)`. `n = 0` / `1.5` / `10000` hydrates. Tune rejects those; parse does not. `counterDial` then does `count / n`. No migrate, so this is the 1.71 file shape.
- Suggestion: Reject unless `Number.isInteger(n) && n >= 1 && n <= COUNTER_MAX` (and integer `count >= 0`). Same gate as `tuneCounterBody`.
- Status: open

### Issue 2 — Severity: bug
- File: src/App.tsx:930
- Description: `dashCargo` `find`s `driven.hitch`, then `if (hitch === undefined) return []`. Tractor `hitch !== 'none'` without that trailer is unrepresentable. Silent empty strip when the relation is wrong. `Dash` already `find`s the same hitch for `{used}/100` (`:941`) and hides the readout the same way.
- Suggestion: One lookup. Missing hitch throws (or index trailers by id). Occupied Faces from that trailer only.
- Status: open

### Issue 3 — Severity: suggestion
- File: src/game/ui/objecthud.tsx:173
- Description: `hudSpec` has no counter arm (`return undefined`). `ObjectHud` special-cases `{ kind: 'counter' }` and `CounterHud` copies the Chrome / `w-56` / × / map-pin block. Water / harvest / day stay on `HudSpec` + `Btn`. Field is the only extra; the shell is not.
- Suggestion: `HudSpec` += a field row (`name` `n`, parse integer, `tuneCounter`). One `ObjectHud`. Drop `CounterHud`.
- Status: open

### Issue 4 — Severity: suggestion
- File: src/game/sim/world.test.ts:2101
- Description: Invariant 116 test never boards, never recaps, never asserts queue/work clear. Invariant 117 (dash occupied Faces) has no test at all. `_index` tests follow the numbered list.
- Suggestion: Stride while driver: actor stays. Recap: actor stays. Hold W, enqueue walk, stride tick: queue empty. Dash: occupied only, hitch-none none, empty omitted.
- Status: open

### Issue 5 — Severity: bug
- File: src/game/ui/objecthud.tsx:218
- Description: Counter HUD face is title **Counter**, live `count`, **Count to**, `n` Field, **Reset to 0**. `CounterHud` paints that column in order, then the last `Btn` is **Reset**.
- Suggestion: Last row copy **Reset to 0**. Click stays `resetCounter`.
- Status: open
