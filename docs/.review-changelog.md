## Summary

Un-numbered menu version-history: Menu-local `MenuPage`, wordmark toggle, Chrome body, static `RELEASES` identical to `docs/ui/changelog.md`. `SAVE_VERSION` is still 1.3; wordmark is still 1.3.0. No `Panel` arm, Overlay, fetch, docs import, or version bump. Boot × shows on changelog (and join), not boot home; `joining` forces home and JoinFields still wins; boot Esc still only `setJoining(false)`. Render-time `setPage` when `joining` is the React 19 store-from-props pattern, not a defect.

## Issues

None.
