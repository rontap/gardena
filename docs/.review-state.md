## Summary

0.9 log + named RNG matches the contracts. Public UI mutators wrap `dispatch`; `enqueue` does not; `confirmPlace` is not a cmd; `apply` calls bodies (no double-log). Worker holds `Cmd[]` only; Vitest never loads it. `Math.random` is omitted-seed only. Shop/grow/fruit identities match `architecture/rng.md`; `ripenN` lives on `World`; failed buy/bulk/drop consume 0 `next()`. No src comments, no `any`, no invented save/replay UI.

## Issues

### Issue 1 — Severity: suggestion
- File: src/game/sim/world.ts:1273
- Description: Seed-bank fit is checked before `shop.next()`, so it cannot use the granted rarity. `buy` requires an empty slot or all four rarity stacks; `buyPacks` requires `canFitSeeds` of all four rarities. Full inventory with a mergeable common stack refuses a buy that would succeed after a common roll. Bulk with 1–3 empties refuses a grant that would have been one stack. Consume-0 holds; grant-if-it-fits does not.
- Suggestion: Preview `Seq` (or roll then commit `n` only on success). `canFitSeeds` the actual stacks, then `next()` / persist.
- Status: open
