# Version

1.9.1

The only version number in `docs/`. Every other note `[[GLOBAL_VERSION]]`. Never write another version literal.

`SAVE_VERSION`, dump `version`, `PROTOCOL`, and the menu wordmark are identifiers in code. Their digits are not restated anywhere in `docs/` except this file.

Orchestrator owns this file. Children never write a version number.
