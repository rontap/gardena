# i18n

Player strings. English only. Locale `en`, pinned. No picker. [[standards/user-facing-text]]

Paraglide JS. Message files `messages/en/{section}.json`. Compiler emits `src/paraglide/` (gitignored). Sim and React call the same `m.*`.

`npm run dev` / `npm run build` / `npm test` compile through `paraglideVitePlugin`. Edit a JSON, the plugin recompiles, Vite HMR picks it up. `npm run i18n` is the same compile without Vite — CI or a one-shot. Not the edit loop.

`fill()` is gone. Named params on the message. Missing key or missing param is a type error.

Plurals use inlang variants (`match` / `countPlural`), not ICU `{n, plural, …}` — that form does not compile with plugin-message-format@4.

Numbers from `src/game/defs/`. Copy never embeds digits.

Player words for this HUD: **Variety**, **Quality** as a percent, **Preserving**, **Fresh**, **Alcohol**. `tier` is vault-only. A rating number never ships without its path. New and changed strings start `<needs-game-text-writer>`.

## Sections

| file | owns |
|---|---|
| `names` | crop, tree, sku, building, Variety, weather, phase. Path labels **Preserving** / **Fresh** / **Alcohol**. Station `skuLabel`. Graft item. Named jam / spirit of a Variety |
| `catalog` | almanac SKU descriptions. Station description. Crop / tree `CROPS.desc` |
| `prompt` | `prompt.ts` `look.ts` `say()`. Neighbour wait line. Graft. Station look / dump. Mill / jam / barrel / grinder locked-Variety lines |
| `hud` | ribbon, recap, docks, inspect bar labels. **Variety**. **Quality** as percent. Held / tooltip / silo / shop seed rows |
| `skills` | skill name + description |
| `research` | research name + description |
| `almanac` | Overview / Game concepts. Variety page. Quality page. Variety row descriptions |
| `market` | stall, contracts overlay |
| `family` | Family overlay |
| `vehicles` | hangar, dash, routes |
| `sensors` | sensor HUD, wire prompts |
| `menu` | home / gear |
| `tutorial` | tutorial lines |

Keys unique across files. Prefix `catalog_` `prompt_` `hud_` … `names` is the one source for `Pumpjack`, Variety names, path words. Other files do not duplicate those words.

`skuLabel` / `cropName` / `faceName` / Variety name: total maps `Id → message fn`.

Changelog is not a message file. [[architecture/changelog]]

Authorship of new strings: [[agents/game-text-writer]] edits `messages/en/`. Call `m.*` at the use site so a JSON save shows up on the next paint.

## Slots

Copy that does not yet have a message. Coder lands the key with this prefix; game-text-writer rewrites and strips it.

| surface | slot |
|---|---|
| inspect growing / ripe / tree look | `<needs-game-text-writer>` Variety name in the look line; ripe Quality as percent |
| neighbour `lookText` | `<needs-game-text-writer>` names what the stalled plant is waiting for |
| held / tooltip fruit, seeds, tree-seed, graft, goods | `<needs-game-text-writer>` Variety, Quality as percent |
| silo cell / shop seed card | `<needs-game-text-writer>` Variety, Quality as percent |
| mill / jam / barrel / grinder wrong locked | `<needs-game-text-writer>` locked Variety only |
| jam filling / dump named jar | `<needs-game-text-writer>` |
| graft prompt | **Graft** |
| station title, look, dump, panel fields, footer | `<needs-game-text-writer>` |
| almanac Variety / Quality concepts, Variety descriptions, Seeds Overview | `<needs-game-text-writer>` |
