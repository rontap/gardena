# v0.1 architecture

Supersedes [[architecture/beta-6]] where this file names a replacement. First non-beta. Slice: Market. Beta-6 tree / Place / freshness / Face / left shop+research docks stay except below.

Do not create `src/` from this note.

## Tree

```
src/game/
  defs/                  (beta-6)
  sim/
    ids.ts               += StallGoodId
    building.ts          += Truck TRUCK_BASE YARD PAD
    plot.ts              Cell += Truck; isSolid += truck
    gen.ts               RESERVED += truck cells + YARD; place truck
    stall.ts             NEW StallGood stallX tenths rate drift retarget crateCells
    world.ts             Intent consign; dest PAD; stall; sales; -= sell*
    prompt.ts            truck cells → consign; delete truck blocked
    look.ts              truck
    (beta-6 else)
  view/map.tsx           truck; derived yard crates; sale popups
  ui/market.tsx          App overlay, not Dock
  ui/inventory.tsx       no Sell (no sellSlot)
  ui/frame.tsx           Dock: shop / research left. Market is not a Dock
src/App.tsx              Market overlay panel
```

## Units

| unit | owner | shape |
|---|---|---|
| Truck | `World.truck`; both cells hold the same instance | class, `kind: 'truck'` |
| Yard | gen-reserved plots, not a Cell kind | `YARD` coords |
| Pad | walk dest | `PAD` ∈ `YARD` |
| StallGood | `World.stall[id]` | class, one per `StallGoodId` |
| StallSale flush | `World.sales` | this-tick list; sim owns |
| Yard crate | view-derived, not Cell, not on World | `crateCells(seed, stall)` |

## Truck

```
TRUCK_BASE: RectBase = { shape: 'rect', col: 12, row: 8, w: 2, h: 1 }
YARD: Coord[] = [{ col: 12, row: 9 }, { col: 13, row: 9 }, { col: 14, row: 9 }]
PAD: Coord = { col: 12, row: 9 }

class Truck {
  readonly kind = 'truck'
  readonly base: RectBase
}
```

Cells `(12,8)` and `(13,8)` are the truck. Solid. Not a plot. `isSolid` includes `truck`.

Yard cells stay plots (gen: untilled soft). Walkable. Not solid.

```
Cell = Plot | House | Pump | Rock | Shrub | Chest | Grinder | Truck
```

`World.truck: Truck` — same lifetime as `World.house`.

Gen `RESERVED` = house occupied ∪ door ∪ starter pump occupied ∪ truck occupied ∪ `YARD`. Place truck on its cells with house / starter.

`deleteBuilding`: house, starter pump, truck are not targets. Prompt on truck + delete arm: blocked.

## Intent

```
type Intent =
  | { act: 'walk'; at: Coord }
  | { act: 'shovel'; at: Coord }
  | { act: 'mine'; at: Coord }
  | { act: 'plant'; at: Coord }
  | { act: 'water'; at: Coord }
  | { act: 'harvest'; at: Coord }
  | { act: 'fill'; at: Coord }
  | { act: 'consign' }
  | { act: 'pickup'; at: Coord }
  | { act: 'drop'; at: Coord }
  | { act: 'inventory' }
  | { act: 'chest'; at: Coord }
  | { act: 'grind'; at: Coord }
```

Illegal: `{ act: 'sell' }`. Illegal: `at` on `consign`.

```
dest(consign) = PAD
dest(inventory) = DOOR
```

Click **truck cells only** → enqueue `{ act: 'consign' }`. Yard / pad clicks are plot prompts.

`TaskName`: `'Consign'` replaces `'Sell'`.

## Kill

Gone: `SaleOffer`, `saleOffer()`, `doSell()`, `sellSlot()`, `{ act: 'sell' }`.

House inventory Sell dies. No `sellSlot` on `World`.

`unitSale` / `fruitMoney` stay (harvest bake). Stall ignores `unitSale`.

## Stall

```
type StallGoodId = CropId | 'berry'
```

`src/game/sim/ids.ts`. Illegal: seeds on stall. Illegal: any other id.

```
STALL_IDS: StallGoodId[] =
  ['carrot', 'potato', 'wheat', 'tomato', 'raspberry', 'watermelon', 'berry']

goodIx(id) = index in STALL_IDS
```

```
class StallGood {
  readonly id: StallGoodId
  offered: number
  market: number
  target: number
  acc: number
  readonly stock: { [K in Rarity]: number }
}
```

`stock` always has all four rarities. Zero is empty, not missing. Mixed rarities live on one good.

```
type StallMap = { [K in StallGoodId]: StallGood }

World.stall: StallMap
```

Constructed complete. One good per id from tick 0. Not a `Map` (cannot omit a key). Not `Partial`.

```
type StallSale = { good: StallGoodId; rarity: Rarity; money: number }

World.sales: StallSale[]
```

## X / money

```
tenths(n) = Math.floor(n * 10)
```

All offered / rate / money compares use `tenths`.

```
stallX(id, mods):
  id === 'berry' → BERRY_SALE          // 2. No research mul
  else CROPS[id].sale * saleMul(id)
```

`saleMul(crop)` = product of `modifiers` where `crop === undefined || crop === id`, start `1`. Not `RARITY_SALE`. Rarity is paid at stall, not in X.

At construct: `offered = market = target = stallX(id)`. `acc = 0`. `stock` all 0.

Offered ∈ `[0.1, 3X]`, step `0.1`.

```
World.nudgeOffered(id: StallGoodId, dir: 1 | -1): void
  t' = clamp(tenths(offered) + dir, 1, tenths(3 * stallX(id)))
  offered = t' / 10
```

Stall pays `offered * RARITY_SALE[rarity]` per unit. Ignore `unitSale`.

Research sale-mul complete: recompute X for that crop; `market` / `target` clamp into new `[0.25X, 1.75X]`; `offered` unchanged. Berry unchanged.

## Drift / retarget

Every 10s of play `dt` (private beat; pauses in recap): each good, `market` steps `0.1` toward `target`, or arrives if `tenths` equal.

Retarget every good:

- `Clock.t` crosses 120 this tick (`t0 < 120 && t >= 120`) → `slot = 0`
- day seam (`clock.advance` → `'seam'`) → `slot = 1`, then recap as now

```
u = hash(seed, 'mkt-tgt', goodIx(id), clock.day, slot)
target' = clamp(target * (0.75 + u * 0.5), 0.25X, 1.75X)
```

`clock.day` after `advance`. Seam retarget runs on the seam tick before recap freeze. No stall dt during recap.

## Consign

Arrive `PAD`. Then:

Legal cargo (whole lot; count ≥ 1 is enough — partial stacks OK):

- hand `fruit` → lot `{ good: crop, rarity, n: count }`; hand → empty
- hand `berry` → lot `{ good: 'berry', rarity, n: count }`; hand → empty
- box fruit cargo `count ≥ 1` → lot from stack; cargo → empty; box stays in hand
- box berry cargo `count ≥ 1` → lot from cargo; cargo → empty; box stays in hand

Seeds (hand or box), empty box, empty hand: no-op. Hand unchanged.

Lot: `stock[rarity] += n` on `World.stall[good]`.

## Rate / sell

Per good with stock > 0:

```
if tenths(offered) >= tenths(1.75 * market): rate = 0
else rate = min(0.2, 0.1 * market / offered)

acc += rate * dt
while acc >= 1 and stock > 0:
  sell 1 in rarity order common → uncommon → rare → heirloom
  money += offered * RARITY_SALE[rarity]
  sales.push({ good, rarity, money: offered * RARITY_SALE[rarity] })
  acc -= 1
stock 0 → acc = 0
```

Stock = sum of the four rarity counts.

Play `tick`: clear `sales`, then clock / jobs / queue / field / stall. Stall appends this tick's `StallSale`s. Ping if `sales.length > 0`.

View toasts read `World.sales` this frame. No second toast store.

## Yard crates

Not `Cell`. Not stored on `World`.

```
crateCells(seed, stall): { id: StallGoodId; at: Coord }[]
```

`src/game/sim/stall.ts`. View (`map.tsx`) calls it.

Stocked goods only (`stock` sum > 0), in `STALL_IDS` order.

Start index = `Math.floor(hash(seed, 'crate', goodIx(id)) * YARD.length)`. Walk `YARD` circular; first cell not already used this pass. No free cell → no crate for that good.

## Overlay

HUD Market is an App panel overlay. Not a left `Dock`. File `src/game/ui/market.tsx`. Shop / research stay left docks.

`World.nudgeOffered` from plus / minus. Overlay iterates `STALL_IDS`.

## Invariants (do not runtime-check)

- `StallGoodId` is `CropId | 'berry'`; seeds cannot be a stall key
- `World.stall` has every `StallGoodId` and no other
- one `StallGood` per id
- `stock` has all four rarities
- truck is `kind: 'truck'`; yard is plots
- truck cells are solid; pad is not
- house, starter pump, truck are not delete targets
- `{ act: 'sell' }` does not exist
- `sellSlot` / `saleOffer` / `SaleOffer` / `doSell` do not exist
- `dest(consign)` is `PAD`
- consign click is truck cells only
- stall ignores `unitSale`
- berry X is `BERRY_SALE`; no research mul
- crate cells are derived; not World state
- Market overlay is not a `Dock`
- `sales` is this-tick flush only

## E2e hooks

| attr | host |
|---|---|
| `data-truck` | each truck cell in `map.tsx` |
| `data-yard-crate` | each derived crate sprite in `map.tsx` |
| `data-market-overlay` | Market overlay root |
| `data-stall-box="{id}"` | overlay box; `{id}` is `StallGoodId` |
| `data-offered` | offered value inside `data-stall-box` |
| `data-market-price` | market value inside `data-stall-box` |
| `data-plus` | + inside `data-stall-box` → `nudgeOffered(id, 1)` |
| `data-minus` | − inside `data-stall-box` → `nudgeOffered(id, -1)` |
| `data-sale-popup` | each toast from this-tick `World.sales` |
