# Tilled edges

Tilled plots keep the dirt texture continuous across adjacent tilled cells. At every boundary with non-tilled ground, a rotated dirt-lip SVG marks the raised transition. The lip may overhang its 24 px source grid so plot boundaries do not read as hard boxes.

U-turn corners receive an inset connector where three quadrants are tilled and the missing diagonal quadrant is non-tilled.
