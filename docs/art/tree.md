# Trees

`prop-{apple,apricot,lemon,cherry}-tree.svg` are 24×48 vertical two-tile props with `grow` `unripe` `ripe` groups. Fruit icons `fruit-{apple,apricot,lemon,cherry}.svg` are 24×24 with `common` `rare` `heirloom`; uncommon uses common. Pink Lady is the apple `heirloom` group.

Sapling face is the `grow` group.

Both: [[art/svg]], [[art/palette]]. Sim: [[architecture/tree]].
