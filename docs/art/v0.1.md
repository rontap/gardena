# v0.1 art

Supersedes [[art/beta-6]] where this file names a replacement. Palette, 24-unit grid, `shape-rendering="crispEdges"`, no text, no raster, no `currentColor`, no new hex.

Adds three files. Everything else stays [[art/beta-6]]. Do not redraw house, actor, coins, HUD buttons.

## Palette

Tokens from [[art/beta-1]] / [[art/beta-6]]. Unchanged. Fruit red `#c43c3c` stays. No other hex.

| token | hex |
|---|---|
| grass | `#4a7c3f` |
| grass-dark | `#3a6232` |
| dirt | `#8a5a32` |
| dirt-dark | `#6b4423` |
| water | `#3d7ea6` |
| leaf | `#6bc04a` |
| ripe | `#d4a017` |
| ink | `#1c1710` |
| house | `#cfc6b0` |
| roof | `#8b3a2a` |
| fruit-red | `#c43c3c` |

## Truck

`prop-truck.svg` — viewBox `0 0 48 24`. **2×1**. Anchor at `TRUCK_BASE` min-corner `(12, 8)`. Art size **is** the rect base.

Cottage pickup. Cabin north. Bed + tailgate south (bottom of viewBox), toward the yard (row 9). Wood body `dirt` / `dirt-dark` / `ink`. `house` steel bits only (hubs, windshield, bumper, tailgate straps). Not irrigation pipe steel. No driver. No groups.

Readable at 48px/tile.

## Market stall

`ui-market-stall.svg` — viewBox `0 0 240 120`. Market overlay scene. Not a map prop. Not `ui-btn-market.svg`.

Wood stall, `roof` awning, empty till. No boxes. No text. No `idle` / `hover` groups.

Group `id="till"`: empty drawer on the counter. Coder composites `ui-coin.svg` to its right. No coin in the file.

Daughter: small cottage girl at the stall. `house` skin, `dirt-dark` hair, `roof` dress, `fruit-red` bow. Not `actor.svg`. Not on the map.

## Yard crate

`prop-crate.svg` — viewBox `0 0 24 24`. **1×1**. Derived yard sprite, cell min-corner. Not a `Cell`.

Small wooden box. Same wood language as `item-box` / `prop-chest`: `dirt` / `dirt-dark` / `ink`. Slats. No clasp. No lid latch. No fruit baked in. No groups.

## Do not

Imagine, raster, new hex, text in asset files, map daughter, redraw `prop-house.svg` `actor.svg` `ui-coin.svg` `ui-coin-silver.svg` `ui-btn-*.svg`.
