# Sensors

Lever, button, lamp, OR, AND, NOT, water / fertilizer / harvest / water-system sensors, vehicle detector. Smart valve is Water (flow), not this shelf — [[items/irrigation]].

Rules: [[mechanics/sensors]]. Research: [[mechanics/research]].

SKUs: `buy-lever` `buy-button` `buy-lamp` `buy-or` `buy-and` `buy-not` `buy-sensor-water` `buy-sensor-fert` `buy-sensor-harvest` `buy-water-system` `buy-vehicle-detector`.

`buy-smart-valve` is irrigation.

Build shelf **Sensors**, id `logic`. Filing: signal → Sensors. Vehicle detector → Sensors. Smart valve → Water (flow).

`Sku.tab` `automation`. `machine-contracts`. Almanac entries exist; copy [[ui/almanac]].
