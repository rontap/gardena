# Furnace

Spec. Graduates into [[mechanics/machines]], [[mechanics/trees]], [[mechanics/inventory]], [[mechanics/research]], [[mechanics/sensors]], [[mechanics/vehicles]], [[items/buildings]], [[items/tools]], [[items/fertilizer]], [[ui/machines]], [[ui/inspect]], [[ui/place]], [[ui/almanac]], [[ui/recipe]], [[architecture/tree]], [[architecture/tick]], [[architecture/save]], [[architecture/log]], [[architecture/modules]]. Dump identity [[GLOBAL_VERSION]]. No migrate.

Illegal in notes: buffer, beacon, organic-as-soil. Feedstock / `units` / covering furnace. `furnaceMul` is the identifier. No coined third word.

Copy: [[standards/user-facing-text]]. Fire VFX palette is designer. No new hex.

## Furnace

1×2 (`w = 1` `h = 2`), origin NW, no rotate, same instance both cells, tick origin. Place like still: hover is origin, extends south. Disarm. Pay on confirm. `Sku.tab` `automation`. `haggling`. Guest shop + place + delete + dump. Processing shelf, compost group. `dest(furnace)` = origin.

Own research `unlock-furnace`, trade, `effect` `unlock-sku` `buy-furnace`. `reveal` `unlock-fermentation`. `requires` `[]`. Cost / seconds / sku price: preference, mid-end versus `unlock-fermentation` / `buy-still`. Name / `skuLabel` **Furnace**. `buy-furnace` show `start`, buy `unlock-furnace`.

```
Furnace {
  base: RectBase   // w = 1, h = 2
  units: number
  progress: number
  inn: Signal
  out: Signal
  hold: number
}
```

Cap `FURNACE_CAP`. Need `FURNACE_NEED` to tick. `FURNACE_SECONDS` — preference. Finish: `units -= FURNACE_NEED`, drop `{ kind: 'ash'; count: FURNACE_ASH }`, east store else `frontOf` (full / no plot → wait at `progress >= 1`). Leftover stays. Mix freely: no recipe lock.

Dump / west-pull: accept until cap, whole items while `units + furnaceValue <= cap`, sugar liters may partial, still no-overload. `inn === 1` skip tick; dump and Unload still fill. Unwired `inn` 0 ticks.

`out` high iff `units === 0`. `SENSOR_HOLD`. Port `in` origin top, `out` origin bottom. First machine with both. Combinational `inn` like mill. `out` is a world-reader like chest. South cell: no port.

Chest I/O: west of origin input, east of origin output, origin row only. Vehicle pads: dropoff north of origin Unload, takeup south of south cell Load. Blue / green chutes. Index `machines`.

Working: `units >= FURNACE_NEED` and `inn === 0` and `progress < 1`. Prop groups `off` / `on`. State VFX at the south opening while working. Reduced motion: frame 0.

## Feedstock

`furnaceValue(item)` — `FURNACE_VALUE` preference. Refuse → 0, dump refuses.

| item | units |
|---|---|
| rotten, seeds, grass-seeds, tree-seed, weed, grass, dead | `FURNACE_VALUE.green` × count (tree-seed: × 1, no `count`) |
| fruit (any crop, any rarity) | `FURNACE_VALUE.fruit` × count |
| sugar | `FURNACE_VALUE.fruit` × liters |
| oil | `FURNACE_VALUE.oil` × count |
| spirit (any kind) | `FURNACE_VALUE.spirit` × count |
| wood | `FURNACE_VALUE.wood` × count |

Heirloom fruit uses the fruit rate. Jam, cask, flour, extract, compost bags, fertilizer, synth, weed-spray, tools, ash: refuse.

Ash into compost: `COMPOST_VALUE.ash` × count. Compost still counts `COMPOST_NEED` waste. Wood is not compost.

Wood and ash are not stall goods. Count stacks, `STACK_MAX`.

## furnaceMul

Chebyshev ≤ `FURNACE_REACH` between any cell of a **working** furnace and any cell of the target footprint (derived 7 wide × 8 tall from a 1×2). Count `n` working furnaces in reach, **including the target** if it is a working furnace.

`furnaceMul = 1 + FURNACE_HASTE × n`. `FURNACE_HASTE` preference. Progress `+= dt × machineMul() × furnaceMul / work` on mill, jam, grinder. Progress `+= dt × furnaceMul / fixed` on still, compost-box, furnace. Barrel age unchanged. Catalog `clockText` stays nominal seconds. Live `left` uses the tick rate.

A lone working furnace covers itself. Two covering furnaces on a mill: `n = 2`. Empty / filling / `inn === 1` / waiting on output (`progress >= 1`): that furnace is not in `n`.

Snapshot the working set at the start of `tickMachines` (after this tick's `evalSensors`). Two-pass: who would tick, then apply. `tickCompost` uses that same set.

## Axe and wood

`AXES.axe` `{ uses; workSeconds }` — preference. Item `{ kind: 'axe'; usesLeft; workSeconds }`. No `id`. No better-axe. SKU `buy-axe`, utility, unlock + show `unlock-pickaxe`. `unlock-pickaxe` effect stays `buy-pickaxe`. Price preference versus `buy-pickaxe`. `skuLabel` **Axe**.

Intent `{ act: 'chop'; at }`. Either cell of the 1×2. Legal: hand axe, `cell.kind === 'tree'`, `juvenile >= 1`, `trunk === false`. Prompt **Chop**. 0 uses: hand empty. Enqueue, no new `Act` letter.

Complete: `usesLeft -= 1`, drop `{ kind: 'wood'; count: 1 }` `frontOf`, then `trunk = true`, `juvenile = 0`, `fruit = 0`, `yield = pending`, `tended = false`. Pending fruit is lost. Ground drops around the tree stay. Shovel still uproots any tree (including trunk) to a tree seed.

## Trunk loop

`Tree.trunk: boolean` required, starts `false`. Illegal: optional.

Chop → `trunk` (`juvenile` 0..1, `juvenileSeconds`) → `grow` (`trunk = false`, `juvenile` reset 0, another `juvenileSeconds`) → mature `pending`. Two full grows after a chop.

When `trunk && juvenile >= 1`: `trunk = false`, `juvenile = 0`. Then existing juvenile-cross-1 → mature. Same tick must not do both.

Stage: `trunk` → `grow` → `unripe` / `ripe`. Tend still only mature `{ off }`. Fruit timer only mature not pending. `trees.ping`: trunk→grow, grow→mature, fruit drop / blocked ripe. Juvenile increment while `< 1` does not ping.

Inspect: trunk and grow both **Growth** on `juvenile`. Trunk **{Name} tree - trunk**. Grow **{Name} tree - growing**.

## Invariants

- `machines.furnace-feed`
- `machines.furnace-burn`
- `machines.furnace-haste`
- `machines.furnace-io`
- `machines.recipe-haste` (extended)
- `trees.chop`
- `trees.trunk`
- `inventory.ash`
- `research.furnace`

## Assumptions (proceed)

- Sugar furnace units = fruit rate per liter.
- Tree-seed is green rate (listed seeds), even though compost refuses it.
- Research `reveal` fermentation, not pickaxe: wood can stockpile first.
- `furnaceMul` keys off working, not placed. Intake is the cost.
- Consume `FURNACE_NEED` at finish, compost-style.
- Chest I/O on the origin row only.
- Fire VFX uses existing palette tokens. No new hex.
- Dump identity [[GLOBAL_VERSION]]. No migrate.
- `dest(furnace)` = origin.
- Snapshot once at start of `tickMachines`; compost uses that set.
- `buy-furnace` Processing, compost group.
- Chop always completes; wood via `frontOf` / `dropSpot`.
- `COMPOST_VALUE.ash` preference. Compost recipes += ash `one`.
- Axe `Item` has no `id`.
- Guest `GUEST_BUILD` += `buy-furnace`.
